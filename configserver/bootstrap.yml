spring:
  application:
    name: config-server
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      server:
        git:
          uri: https://github.com/dmaronov/cloud-config-repo.git